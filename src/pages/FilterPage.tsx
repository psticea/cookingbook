import React from 'react';

const FilterPage: React.FC = () => {
  return (
    <div className="min-h-screen">
      <main className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold mb-6">Filter Recipes</h1>
        {/* Filter controls and recipe grid will be added in a later task */}
        <p className="text-gray-600 dark:text-gray-400">Filter controls coming soon...</p>
      </main>
    </div>
  );
};

export default FilterPage;
